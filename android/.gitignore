# Build outputs
app/build/
build/
.gradle/
*.apk
*.aab

# Secrets — NUNCA commitar
*.jks
*.keystore
service-account.json

# Android Studio
.idea/
*.iml
local.properties
.DS_Store

# Gradle wrapper cache (manter o jar que é necessário para o CI)
# gradle/wrapper/gradle-wrapper.jar  ← NÃO ignorar, CI precisa dele
