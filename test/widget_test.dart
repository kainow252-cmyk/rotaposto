import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('RotaPosto app smoke test', (WidgetTester tester) async {
    // Smoke test básico — app nativo sem WebView
    expect(true, isTrue);
  });
}
